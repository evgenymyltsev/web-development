extends layout
block main
    if username
        h1 Привет #{username}
        a(href='/suggestions') Посмотреть список предложений
    else
        h1 Вход
        form(method="POST")
            input(type="text" name="username")
            button(type="submit") Войти